<?xml version="1.0" encoding="UTF-8"?>
<trackingContexts>
	<default>
		<param name="interval" value="10s"/>
		<pings>
	        <contentPlay>
	            <param name="ping" value="${video.id}/tracking.vidt"/>
	        </contentPlay>
	        <contentReplay>
	            <param name="ping" value="${video.id}/tracking.vidt"/>
	        </contentReplay>
	    </pings>
		<omniture>
			<dataPoints>
			</dataPoints>
		</omniture>
	</default>

    <!-- make sure "autostart" context is in trackingPolicy.xml since we use this.setTrackingContext('autostart'); in player.js, otherwise, autostart tracking will break -->
    <context name="main">
        <omniture>
            <dataPoints>
            </dataPoints>
        </omniture>
    </context>
    <context name="autostart">
        <omniture>
            <dataPoints>
            </dataPoints>
        </omniture>
    </context>

    <context name="twitter">
		<omniture>
			<param name="omnitureAccount" value="aolturnercnnmoney-adbp-offsite"/>
			<param name="omnitureServer" value="https://smetrics.cnn.com"/>
			<dataPoints>
			</dataPoints>
		</omniture>
	</context>

</trackingContexts>