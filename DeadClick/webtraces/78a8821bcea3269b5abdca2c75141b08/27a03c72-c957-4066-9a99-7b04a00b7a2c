_satellite.pushBlockingScript(function(event, target, $variables){
  //**************************************************
//     Sublime Friendly - RC START
//**************************************************
/*
// "Make an offer" submission
var radioResult = '', isMobile = false;																			 				

var checkIfMobile = function(){ isMobile = document.documentElement.clientWidth <= 920; };						 				

var updateElPresence = function(){																     			 				
	checkIfMobile();																			     			 				
	if(isMobile){
		//console.log('In mobile');
		$(document).on('touchstart', '.makeoffer_submit', function(e){
				$(this).parents('.offer-content').find('.RadioButton').each(function(){							 				
					var radioSelected = $(this).is('.isSelected');												 				
						if(radioSelected){
							var lotViewLotNum = $(this).parents('.lotdetail-MakeAnOffer').attr('data-lot-id'),
								listViewLotNum = $(this).parents('.sale-article').attr('data-lot'),				 	 			
								saleNum = (s.eVar15 !== undefined) ? s.eVar15 : $('.ajax-watchlive').attr('data-salenumber');
							var lotNum = (lotViewLotNum !== undefined) ? lotViewLotNum : listViewLotNum;		 				
							var dataLot = $(this).attr('data-lot')
							if(dataLot !== undefined){															 				
								radioResult = (dataLot === 'phone') ? 
								'Make An Offer:Phone Number:' + saleNum + ':' + lotNum:
                'Make An Offer:Email:' + saleNum  + ':' + lotNum;
								s.eVar34 = radioResult;															 				
								s.events = 'event89';
                //s.linkTrackVars = s.apl(s.linkTrackVars, 'eVar34', ',' ,1);
				        s.linkTrackVars= 'events,eVar32,eVar34,prop32,eVar61';
				        s.eVar61 = s.prop32 = "Make an offer Submit"
        				s.tl(true,'o','Make an offer submit');
        				console.log(radioResult);
								console.log('event89 fires');
							}
						}
				});
		});		
	} else {
		//console.log('In desktop');
		$(document).on('click', '.makeoffer_submit', function(){
			
			$(this).parents('.offer-content').find('.RadioButton').each(function(){							 	 				
				var radioSelected = $(this).is('.isSelected');												 	 				
					if(radioSelected){
						var lotViewLotNum = $(this).parents('.lotdetail-MakeAnOffer').attr('data-lot-id'),
							listViewLotNum = $(this).parents('.sale-article').attr('data-lot'),				 	 				
							saleNum = (s.eVar15 !== undefined) ? s.eVar15 : $('.ajax-watchlive').attr('data-salenumber');
						var lotNum = (lotViewLotNum !== undefined) ? lotViewLotNum : listViewLotNum;		 	 				
						var dataLot = $(this).attr('data-lot')
						if(dataLot !== undefined){															 	 				
							radioResult = (dataLot === 'phone') ? 
							'Make An Offer:Phone Number:' + saleNum + ':' + lotNum:
							'Make An Offer:Email:' + saleNum  + ':' + lotNum;
							s.eVar34 = radioResult;															 	 				
							s.events = 'event89';
				      s.linkTrackVars= 'events,eVar32,eVar34,prop32,eVar61';
				      s.eVar61 = s.prop32 = "Make an offer Submit"
        			s.tl(true,'o','Make an offer submit');
							console.log(radioResult);
							console.log('event89 fires');
						}
					}
			});
		});
	}
};
window.addEventListener('resize', updateElPresence);											     			 				// --- UPDATE whether the Make Offer Button will track clicks based on a visitor resizing his or her browser
updateElPresence();		

*/
});
