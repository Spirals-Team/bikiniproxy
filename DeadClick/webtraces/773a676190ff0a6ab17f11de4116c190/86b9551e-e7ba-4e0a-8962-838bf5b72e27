// JavaScript Document
$j=jQuery.noConflict();
$j(document).ready(function(){		
	$j('.post').cornerz({radius:10, background: '#f7f8f8'});
	$j('.img_cornerz').cornerz({radius:14, background: '#E3EFED'});
	$j('.wp-caption').cornerz({radius:14, background: '#fff'});
	$j('.menu ul li h3').cornerz({radius:8, background: '#f7f8f8', corners:"tl tr"});
	$j('.small-promo h2').cornerz({radius:8, background: '#f7f8f8'});
	$j('.large-promo h2').cornerz({radius:8, background: '#f7f8f8'});
	$j('h3.archive_title').cornerz({radius:8, background: '#f7f8f8', corners:"tl tr"});
	$j('#navigation').cornerz({radius:10, background: '#f7f8f8'});
	$j('blockquote').cornerz({radius:14, background: '#fff'});
	$j('#login-search-box li.bg').cornerz({radius:3, background: '#fff'});
	$j('.about_header').cornerz({radius:10, background: '#f7f8f8'});
	$j('.about_post_inner').cornerz({radius:10, background: '#f7f8f8'});	
	
	//COMMENT STRIPES
	$j("ul#commentlist li:nth-child(even)").addClass("row");
	
/*
    if ($j.browser.msie) $j('select.postform')
    .bind('mouseover', function() { $j(this).addClass('expand').removeClass('clicked'); })
    .bind('click', function() { $j(this).toggleClass('clicked'); })
    .bind('mouseout', function() { if (!$j(this).hasClass('clicked')) {         $(this).removeClass('expand'); }})
    .bind('change blur', function() { $j(this).removeClass('expand clicked'); });
*/

/*
    $j('.megamenu .l2Cols h2 a').each(function (i) {
    // Get the text of each link and trim any whitespace
        var contents = jQuery.trim($j(this).html());
    // Split into an array
        var a = contents.split(/\s/);				
    // Wrap the last word in a span tag
        a[a.length - 1] = '<span>' + a[a.length - 1] + '</span>';
    // Join the array back together and update the html text of the link
        $j(this).html(a.join(' '));			
    });	 
*/
});