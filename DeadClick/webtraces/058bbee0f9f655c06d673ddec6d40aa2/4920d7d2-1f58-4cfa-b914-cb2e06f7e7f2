/* Base */

/* Basic style
---------------------------------------- */

*, *:before, *:after {
    box-sizing: inherit;
}

html {
    box-sizing: border-box;
}

body {
    font-size: 16px;
    line-height: 1.6;
    background: #fff;
    color: #333;
    margin: 0;
}

h1, h2, h3, h4, h5, h6 {
    line-height: 1.25;
    margin: 0 0 14px;
}

h1 {
    font-size: 36px;
    font-weight: normal;
}

h2 {
    font-size: 20px;
}

h3 {
    font-size: 16px;
}

h4, h5, h6 {
    font-size: inherit;
}

a {
    text-decoration: none;
    border-bottom: 1px solid #999;
    color: inherit;
    transition: all .2s;
}

a:hover {
    text-decoration: none;
    border-bottom: 0;
    color: #df4747;
}

h1 a, h2 a, h3 a, h4 a, h5 a, h6 a {
    text-decoration: none;
    border-bottom: 0;
}

address, blockquote, dd, dl, fieldset, form, iframe, ol, p, pre, table, ul {
    margin: 0 0 20px;
}

address {
    font-style: normal;
}

blockquote {
    font-style: italic;
    border-left: 4px solid #ccc;
    padding: 0 20px;
}

code, kbd, pre, samp {
    background: #eee;
    font-size: 14px;
}

code, kbd, samp {
    padding: 4px;
}

dt {
    font-weight: bold;
}

dd {
    margin-left: 0;
}

embed, iframe, object, video {
    max-width: 100%;
}

hr {
    border: 0;
    border-bottom: 1px solid #999;
    margin-top: 40px;
    margin-bottom: 40px;
}

img {
    max-width: 100%;
    height: auto;
}

input[type="text"],
input[type="email"],
input[type="url"],
input[type="password"],
input[type="search"],
textarea {
    font-size: 14px;
    line-height: normal;
    padding: 8px;
    color: inherit;
    background-color: #fff;
    border: 1px solid #ccc;
    border-radius: 0;
    box-shadow: inset 0 1px 2px rgba(0,0,0,.075);
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="url"]:focus,
input[type="password"]:focus,
input[type="search"]:focus,
textarea:focus {
    outline: 0;
    border-color: #999;
    box-shadow: inset 0 1px 2px rgba(0,0,0,.075), 0 0 5px rgba(0,0,0,.25);
}

pre {
    line-height: 1.4;
    padding: 8px;
}

table {
    width: 100%;
    max-width: 100%;
    border-spacing: 0;
    border-collapse: collapse;
}

caption, th, td {
    padding: 8px;
}

th, td {
    text-align: left;
    padding: 8px;
    border: 1px solid #ccc;
}

ul, ol {
    padding: 0;
}

li {
    margin-left: 40px;
}

li > ul, li > ol {
    margin-bottom: 0;
}

/* Typography
---------------------------------------- */

body,
.page-title {
    font-family: "merriweather", serif;
}

h1, h2, h3, h4, h5, h6,
.entry-meta .date,
.articles-sidebar .widget_categories li a {
    font-family: "merriweather sans", serif;
}

.site-header,
.nav-links,
.more-link {
    font-family: "oswald", sans-serif;
}

.header-title .tagline,
.header-navigation {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

/* Site layout
---------------------------------------- */

.site-header {
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 50px;
}

.site-content {
    margin-bottom: 30px;
}

.unit-container {
    margin-left: auto;
    margin-right: auto;
    width: 960px;
}

.content-main {
    width: 600px;
    margin: auto;
}

.content-sidebar {
    float: right;
    width: 260px;
    padding-top: 30px;
}

/* Header area
---------------------------------------- */

.header-title {
    font-size: 22px;
    padding-top: 15px;
    float: left;
}

.header-title .branding {
    text-decoration: none;
    border-bottom: 0;
}

.header-title .tagline {
    display: block;
    text-transform: none;
    letter-spacing: .5px;
    font-size: 14px;
    color: #999;
}

.header-info {
    float: right;
}

.header-navigation {
    font-size: 16px;
    float: right;
}

.header-navigation ul {
    margin: 0;
}

.header-navigation li {
    list-style: none;
    display: inline-block;
}

.header-navigation a {
    display: block;
    text-decoration: none;
    border-bottom: 0;
    padding: 18px 0;
    border-top: 5px solid white;
    color: #999;
}

.header-navigation a:hover,
.current_page_parent a,
.current-menu-item a {
    border-top: 5px solid #df4747;
    color: inherit;
}

.hamburger-checkbox,
.hamburger-label {
    display: none;
}

/* Content area
---------------------------------------- */

.page-header {
    border-bottom: 4px solid #333;
    margin-bottom: 30px;
}

.page-title {
    font-size: inherit;
}

.page-content,
.entry-wrapper {
    margin-bottom: 40px;
}

.entry-content h1,
.entry-content h2,
.entry-content h3,
.entry-content h4,
.entry-content h5,
.entry-content h6 {
    margin-top: 50px;
}

.entry-content h1:first-child,
.entry-content h2:first-child,
.entry-content h3:first-child,
.entry-content h4:first-child,
.entry-content h5:first-child,
.entry-content h6:first-child {
    margin-top: 0;
}

.page-links {
    word-spacing: 4px;
    clear: both;
}

.edit-link {
    clear: both;
}

.continue-link {
    text-decoration: none;
    border-bottom: 0;
}

.more-link {
    text-transform: uppercase;
    margin-bottom: 40px;
}

.page-id-20 .entry-content li {
    list-style: none;
    margin: 0;
}

/* Entry meta
---------------------------------------- */

.entry-meta {
    margin-bottom: 20px;
    font-size: 14px;
}

.entry-meta:empty {
    display: none;
}

.entry-meta .date {
    color: #666;
}

.entry-footer .entry-meta {
    background: #eee;
    padding: 10px;
    margin-top: 10px;
}

/* Comment form
---------------------------------------- */

.comment-respond {
    margin-bottom: 40px;
}

.comment .comment-respond {
    margin-top: 20px;
}

.comment-reply-title small {
    font-size: 14px;
    font-weight: normal;
}

.comment-form {
    margin-bottom: 0;
}

.comment-form .required {
    color: crimson;
}

.comment-form label {
    display: block;
    margin-bottom: 4px;
}

.comment-form input[type="text"],
.comment-form input[type="email"],
.comment-form input[type="url"] {
    width: 50%;
}

.comment-form textarea {
    width: 100%;
}

.comment-form .form-allowed-tags {
    display: none;
}

.comment-form .form-submit {
    margin-bottom: 0;
}

/* Comment list
---------------------------------------- */

.comments-area {
    margin-bottom: 40px;
}

.comment-list {
    margin-bottom: 40px;
}

.comment-list .pingback,
.comment-list .comment {
    list-style: none;
}

.comment-list .pingback {
    font-size: 14px;
}

.comment-list .depth-1 {
    margin-left: 0;
    margin-bottom: 0;
}

.pingback .comment-body {
    padding-bottom: 20px;
}

.comment .comment-body {
    position: relative;
    margin-top: 30px;
    padding-left: 50px; /* avatar 40x40 */
}

.comment-body .reply {
    margin-top: -10px;
}

.comment-author .avatar {
    border-radius: 50%;
    position: absolute;
    left: 0;
    top: 0;
}

.comment-author .fn {
    font-weight: bold;
    font-style: normal;
}

.comment-author .says {
    display: none;
}

.bypostauthor .fn:before {
    margin-right: 4px;
    content: "\2605";
}

.comment-meta {
    font-size: 14px;
    margin-bottom: 12px;
}

.comment-meta a {
    color: #666;
}

.comment-reply-link {
    font-size: 14px;
}

/* Pagination
---------------------------------------- */

.nav-links {
    margin-bottom: 40px;
}

.nav-previous,
.nav-next {
    width: 50%;
}

.nav-previous {
    float: left;
}

.nav-next {
    float: right;
    text-align: right;
}

.posts-navigation {
    text-transform: uppercase;
    text-align: center;
    margin-bottom: 60px;
    clear: both;
}

.posts-navigation .page-numbers {
    border: 0;
    display: inline-block;
    min-width: 36px;
    padding: 5px 8px;
    margin: 0 0 4px;
    background: #fff;
    transition: all .2s;
    background: #eee;
    border-radius: 3px;
}

.posts-navigation .dots {
    background: transparent;
}

.posts-navigation .current,
.posts-navigation a.page-numbers:hover {
    background: #df4747;
    color: #fff;
    text-decoration: none;
}

/* Sidebar area
---------------------------------------- */

.content-sidebar .widget {
    margin-bottom: 30px;
}

.content-sidebar h2 {
    font-size: 16px;
}

.content-sidebar li {
    list-style: none;
    margin-left: 0;
}

.content-sidebar .search-form {
    margin-bottom: 35px;
}

.content-sidebar .widget_text {
    font-size: 14px;
}

.content-sidebar .widget_text p {
    overflow: hidden;
    clear: both;
}

.content-sidebar .text-3 a {
    text-decoration: none;
    border-bottom: 0;
}

.content-sidebar .text-4 {
    margin-bottom: 20px;
}

/* Search form
---------------------------------------- */

.search-form {
    position: relative;
    max-width: 90%;
}

.search-field {
    width: 100%;
}

.search-submit {
    position: absolute;
    left: auto;
    right: 4px;
    top: 0;
    bottom: 0;
    margin: auto;
    width: 24px;
    height: 24px;
    background: #fff;
    border: 0;
}

.search-submit:focus {
    outline: none;
}

.search-submit svg {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    margin: auto;
    width: 60%;
    height: 60%;
    opacity: .75;
    transition: opacity .2s;
}

.search-submit:hover svg {
    opacity: .5;
}

/* Footer area
---------------------------------------- */

.site-footer .unit-container {
    border-top: 4px solid #333;
    padding: 15px 0 30px;
}

.footer-meta {
    float: right;
}

/* Front page
---------------------------------------- */

.home .content-main {
    float: left;
}

/* Articles area
---------------------------------------- */

.articles-sidebar,
.articles-main {
    float: none;
    width: auto;
}

.articles-sidebar {
    padding-top: 0;
}

.articles-sidebar .widget_categories {
    border-bottom: 4px solid #333;
    padding-bottom: 30px;
    margin-bottom: 40px;
}

.articles-sidebar .widget_categories li {
    display: inline-block;
}

.articles-sidebar .widget_categories li a {
    font-size: 13px;
    display: block;
    background: #eee;
    color: #444;
    border: 0;
    border-radius: 15px;
    padding: 2px 8px 0;
    margin: 0 2px 6px 0;
    min-width: 40px;
    text-align: center;
    text-decoration: none;
}

.blog .articles-sidebar .cat-item-all a,
.articles-sidebar .widget_categories .current-cat a,
.articles-sidebar .widget_categories li a:hover {
    background: #df4747;
    color: #fff;
}

.articles-sidebar .widget_categories .cat-title {
    font-size: 13px;
    font-weight: bold;
    text-transform: uppercase;
    margin-right: 4px;
    margin-bottom: 0;
    display: inline;
}

.articles-sidebar .widget_categories .cat-list {
    display: inline;
}

.article-list .entry-wrapper {
    float: left;
    width: 48%;
}

.article-list .entry-wrapper:nth-child(odd) {
    margin-right: 4%;
    clear: left;
}

/* Clear floats
---------------------------------------- */

.clearfix:before,
.clearfix:after,
.site-header:before,
.site-header:after,
.site-content:before,
.site-content:after,
.site-footer:before,
.site-footer:after,
.unit-container:before,
.unit-container:after,
.entry-content:before,
.entry-content:after,
.comment-content:before,
.comment-content:after,
.nav-links:before,
.nav-links:after {
    content: "";
    display: table;
}

.clearfix:after,
.site-header:after,
.site-content:after,
.site-footer:after,
.unit-container:after,
.entry-content:after,
.comment-content:after,
.nav-links:after {
    clear: both;
}

/* Jetpack setups
---------------------------------------- */

.infinite-scroll .posts-navigation,
.infinite-scroll.neverending .site-footer {
    display: none;
}

.infinity-end.neverending .site-footer {
    display: block;
}

/* Media elements
---------------------------------------- */

.hentry .mejs-container,
.hentry .wp-embedded-video,
.hentry .wp-playlist {
    margin: 0 0 20px;
}

.hentry .wp-embedded-video {
    display: inline-block;
}

/* WP default
---------------------------------------- */

.screen-reader-text {
    border: 0;
    clip: rect(0 0 0 0);
    height: 1px;
    margin: -1px;
    overflow: hidden;
    padding: 0;
    position: absolute;
    width: 1px;
}

.screen-reader-text.focusable:active,
.screen-reader-text.focusable:focus {
    clip: auto;
    height: auto;
    margin: 0;
    overflow: visible;
    position: static;
    width: auto;
}

.sticky {
    display: block;
}

.alignleft {
    float: left;
    margin: 4px 16px 12px 0;
}

.aligncenter {
    margin: 0 auto 20px;
    display: block;
}

.alignright {
    float: right;
    margin: 4px 0 12px 16px;
}

.wp-caption {
    text-align: center;
    margin-bottom: 20px;
    max-width: 100%;
    height: auto;
}

.wp-caption-text,
.gallery-caption {
    padding-top: 10px;
    padding-bottom: 10px;
    margin-bottom: 0;
}

/* Print style
---------------------------------------- */

@media print {
}
