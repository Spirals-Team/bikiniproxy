/**
 *  * Akamai Edge Side Scripting 
 *  */
var akamai_esi_geolocation = {
<esi:choose>
  <esi:when test="$(GEO{'country_code'})">
    country_code:"$(GEO{'country_code'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'region_code'})">
    region_code:"$(GEO{'region_code'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'city'})">
    city:"$(GEO{'city'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'areacode'})">
    areacode:"$(GEO{'areacode'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'lat'})">
    lat:"$(GEO{'lat'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'long'})">
    lon:"$(GEO{'long'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'county'})">
    country:"$(GEO{'county'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'timezone'})">
    timezone:"$(GEO{'timezone'})",
  </esi:when>
</esi:choose>
<esi:choose>
  <esi:when test="$(GEO{'throughput'})">
    throughput:"$(GEO{'throughput'})",
  </esi:when>
</esi:choose>
};

