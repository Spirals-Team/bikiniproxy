<div class="sticky-add-to-cart hide">
	<div class="sticky-add-to-cart-container">
		{{#if img.src}}
		<div class="product-swatch">
			<img alt="{{img.alt}}" class="{{img.class}}" src="{{img.src}}" />
		</div>
		{{/if}}
		<div class="product-text">
			<span class="product-name">{{name}}</span>
			{{#if color}}
			<span class="product-color">{{color}}</span>
			{{/if}}
			{{#if size}}
			<span class="product-size">{{size}}</span>
			{{/if}}
			{{#if price}}
			<span class="product-price">{{{price}}}</span>
			{{/if}}
		</div>
		<div class="product-button">
			<a href="#" id="sticky-add-to-cart-button">
				{{#if buttonText}}
				<span class="sticky-text">{{buttonText}}</span>
				{{else}}
				<span class="sticky-icon icon-shopcart"></span>
				{{/if}}
			</a>
		</div>
	</div>
</div>
